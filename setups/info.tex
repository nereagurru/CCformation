Each directory is the setup for a different simulation.
Each file should contain:
- Makefile
- setup.par
- ifdef.opt
- directory.sh

In case of restarting a simulation, the "restart.h5" file should be included together with the aforementioned files,
